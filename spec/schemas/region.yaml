components:
  schemas:
    Region:
      type: object
      description: |
        Represents a region, which is a geographical location
        with one or more zones.
      required:
      - spec
      properties:
        metadata:
          $ref: './resource.yaml#/components/schemas/GlobalResourceMetadata'
        spec:
          $ref: './region.yaml#/components/schemas/RegionSpec'

    RegionSpec:
      type: object
      description: |
        The specification of a region, including the available zones and providers.
      required:
      - availableZones
      - providers
      properties:
        availableZones:
          description: |
            The list of zones available in the region.
          type: array
          minItems: 1
          maxItems: 32
          items:
            $ref: '#/components/schemas/Zone'
          example: ["a", "b", "c"]
          x-oapi-codegen-extra-tags:
            x-kubebuilder-validation-items-min-length: "1"
            x-kubebuilder-validation-items-max-length: "32"
            x-kubebuilder-validation-min-items: "1"
            x-kubebuilder-validation-max-items: "32"

        providers:
          description: |
            The list of providers available in the region.
          type: array
          minItems: 1
          maxItems: 64
          items:
            $ref: './region.yaml#/components/schemas/Provider'
          x-oapi-codegen-extra-tags:
            x-kubebuilder-validation-min-items: "1"
            x-kubebuilder-validation-max-items: "64"

    Zone:
      type: string
      description: Reference to a specific zone within a region
      minLength: 1
      maxLength: 32
      example: "a"

    Provider:
      type: object
      description: |
        A provider of cloud services
      required:
      - name
      - version
      - url
      properties:
        name:
          type: string
          minLength: 1
          maxLength: 64
          example: "seca.network"
          x-oapi-codegen-extra-tags:
            x-kubebuilder-validation-min-length: "1"
            x-kubebuilder-validation-max-length: "64"
        version:
          type: string
          minLength: 1
          maxLength: 16
          example: "v1"
          x-oapi-codegen-extra-tags:
            x-kubebuilder-validation-min-length: "1"
            x-kubebuilder-validation-max-length: "16"
        url:
          type: string
          minLength: 1
          maxLength: 4000
          example: "https://demo.secapi.cloud/providers/seca.network"
          x-oapi-codegen-extra-tags:
            x-kubebuilder-validation-min-length: "1"
            x-kubebuilder-validation-max-length: "4000"
